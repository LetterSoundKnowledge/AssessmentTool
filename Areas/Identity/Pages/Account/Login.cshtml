@page
@model LoginModel

@{
    ViewData["Title"] = "Logg inn";
}

<div class="row mx-0 h-100">
    <div class="col-12 align-self-center">
        <div class="form-content">
            <form id="account" method="post">
                <div class="d-flex justify-content-center">
                    <img src="~/img/Logo.svg" style="width:50%; height:50%" class="filter-light-green mb-1" />
                </div>
                <h2 class="light-green">Logg inn med eksisterende bruker</h2>
                <hr />
                <partial name="_StatusMessage" for="ErrorMessage" />
                @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>*@
                <div class="form-floating">
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" />
                    <label asp-for="Input.Email" class="form-label">E-postadresse</label>
                    <span asp-validation-for="Input.Email" class="light-green"></span>
                </div>
                <div class="form-floating">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" />
                    <label asp-for="Input.Password" class="form-label">Passord</label>
                    <span asp-validation-for="Input.Password" class="light-green"></span>
                </div>
                <div>
                    <div class="checkbox">
                        <label asp-for="Input.RememberMe" class="form-label light-green">
                            <input class="form-check-input" asp-for="Input.RememberMe" />
                            @Html.DisplayNameFor(m => m.Input.RememberMe)
                        </label>
                    </div>
                </div>
                <div>
                    <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary">Logg inn</button>
                </div>
                <div class="mt-2">
                    <p>
                        <a class="light-green" asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Registrer ny bruker</a>
                    </p>
                </div>
            </form>
        </div>
    </div>
</div>
@section Scripts {
<partial name="_ValidationScriptsPartial" />
}
